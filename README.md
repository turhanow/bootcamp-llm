```markdown
# 🚀 Запуск проекта `bootcamp-llm`

Проект поднимает Telegram-бота с in-memory базой данных DuckDB, которая инициализируется из `vacancies.json` при старте приложения.

---

## 📦 Предварительные требования


### 1️⃣ Python

- **Версия:** `Python 3 или выше`
- Проверка:
```bash
python3 --version
```

---

### 2️⃣ Make
- macOS / Linux — установлен по умолчанию
- Проверка:
```bash
make --version
```

---

### 3️⃣ Git (опционально)
Если проект клонируется из репозитория:
```bash
git --version
```

---

## 📁 Данные

Убедитесь, что файл с данными существует:

```bash
data/vacancies.json
```

Это **обязательный файл**, из которого при старте загружается in-memory база данных.

---

## ▶️ Запуск проекта

### 1️⃣ Клонирование репозитория (если необходимо)

```bash
git clone git@github.com:turhanow/bootcamp-llm.git
cd bootcamp-llm
```

---

### 2️⃣ Запуск одной командой

```bash
make start
```

Команда автоматически:

- создаёт виртуальное окружение `.venv`
- устанавливает все зависимости
- запускает приложение

---

## 📟 Логи при успешном запуске

Пример ожидаемого вывода в консоль:

```text
🚀 Application starting
🧠 Process PID: 12345
📦 Loading data into in-memory DuckDB
📊 Tables loaded: ['Vacancies', 'Locations', 'Skills', ...]
📈 Vacancies loaded: 12834
✅ DB check passed: {'tables': [...], 'vacancies': 12834}
🤖 Application ready
```

После этого Telegram-бот начинает работу и процесс остаётся активным.

---

## 🛑 Остановка приложения

Для остановки нажмите:

```text
Ctrl + C
```

---

## ♻️ Чистый перезапуск

Для повторного чистого запуска выполните:

```bash
make start
```

Каждый запуск:

- создаёт новый процесс
- инициализирует новую in-memory БД
- повторно загружает данные из `vacancies.json`

---

## 🧪 Полезные команды

### Проверка активного Python

```bash
which python
```

Ожидаемый результат:

```text
.../bootcamp-llm/.venv/bin/python
```

---

## ⚠️ Важные особенности

- База данных **не сохраняется на диск**
- Все данные живут **только в памяти процесса**
- При каждом старте БД инициализируется заново
- Проверка БД возможна только внутри текущего процесса

---

## 🧠 Архитектурная заметка

In-memory DuckDB инициализируется **до запуска Telegram-бота**, чтобы:

- гарантировать наличие данных
- обеспечить готовую схему
- сделать старт воспроизводимым

---

## 📌 Возможные проблемы

### ❌ Нет логов при старте
Убедитесь, что приложение запускается через:

```bash
make start
```

а не напрямую из IDE.

---

### ❌ `ModuleNotFoundError`
Переустановите зависимости:

```bash
make install
```

---

## ✅ Готово

Проект готов к использованию и дальнейшему развитию 🚀
```
