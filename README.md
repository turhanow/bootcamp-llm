# 🚀 Запуск проекта `bootcamp-llm`

Проект поднимает Telegram-бота с in-memory базой данных DuckDB, которая инициализируется из `vacancies.json` при старте
приложения.

---

## 📦 Предварительные требования

### 1️⃣ Python

- **Версия:** `Python 3 или выше`
- Проверка:

```bash
python3 --version
```

---

### 2️⃣ Make

- macOS / Linux — установлен по умолчанию
- Проверка:

```bash
make --version
```

---

### 3️⃣ Git

Если проект клонируется из репозитория:

```bash
git --version
```

---

### 4️⃣ Git LFS (обязательно)

В проекте используется **Git LFS** для хранения большого файла с данными.

Установите Git LFS и инициализируйте его:

```bash
git lfs install
```

Проверка:

```bash
git lfs version
```

---

## 📁 Данные

Файл с данными:

```bash
data/vacancies.json
```

⚠️ **Важно**  
Этот файл хранится через **Git LFS**.  
Если Git LFS не установлен, вместо данных вы увидите небольшой текстовый файл-заглушку.

Если данные не скачались автоматически, выполните:

```bash
git lfs pull
```

---

## ▶️ Запуск проекта

### 1️⃣ Клонирование репозитория

```bash
git clone git@github.com:turhanow/bootcamp-llm.git
cd bootcamp-llm
```

После клонирования убедитесь, что данные загружены:

```bash
ls -lh data/vacancies.json
```

Размер файла должен быть **существенным**, а не ~100 байт.

---

### 2️⃣ Запуск одной командой

```bash
make start
```

Команда автоматически:

- создаёт виртуальное окружение `.venv`
- устанавливает все зависимости
- загружает данные в in-memory DuckDB
- запускает приложение и Telegram-бота

---

## 📟 Логи при успешном запуске

Пример ожидаемого вывода в консоль:

```text
🚀 Application starting
📦 Loading data into in-memory DuckDB
📊 Tables loaded: ['Vacancies', 'Locations', 'Skills', ...]
📈 Vacancies loaded: 12834
✅ DB check passed: {'tables': [...], 'vacancies': 12834}
🤖 Application ready
```

---

## 🛑 Остановка приложения

Для остановки нажмите:

```text
Ctrl + C
```

---

## ♻️ Чистый перезапуск

```bash
make start
```

Каждый запуск:

- создаёт новый процесс
- инициализирует новую in-memory БД
- повторно загружает данные из `vacancies.json`

---

## ⚠️ Важные особенности

- База данных **не сохраняется на диск**
- Все данные живут **только в памяти процесса**
- При каждом старте БД инициализируется заново

---

## 🧠 Архитектурная заметка

In-memory DuckDB инициализируется **до запуска Telegram-бота**, чтобы:

- гарантировать наличие данных
- обеспечить готовую схему
- сделать старт воспроизводимым

---

## 📌 Возможные проблемы

### ❌ Данные не загрузились

```bash
git lfs pull
```

---

### ❌ `ModuleNotFoundError`

```bash
make install
```

---

## ✅ Готово

Проект готов к использованию и дальнейшему развитию 🚀
